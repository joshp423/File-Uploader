<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Create Folder</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('../partials/nav') %>
    <div class="createFolderForm">
        <a href="/view-files/user/<%= user %>/folder/<%= selectedFolder.id %>"><button>Back</button></a>
        <form method="post" action="/create-folder/user/<%= user %>/folder/<%= selectedFolder.id %>">
            <h1>Create New Folder</h1>
            <label for="name">New Folder Name</label>
            <input required id="name" name="name" placeholder="Folder Name" type="text" />
            <button type="submit">Create Folder</button>
        </form>
    </div>
</body>
</html>
